/**
 * Instructions:
 * 1. Require the custom module "01-module-a" created previously (store in a variable, say, module01) - note that the leading "./"" in the path is necessary, else Node will look for it in node_modules/ folder
 * 
 * 2.1 Try logging the values of variables local to 01-module-a - this will result in error
 * 2.2 Do the same for "global" and properties set on "global" - these can be used in this module too
 * 
 * 3.1 Log the value of "module"
 * 3.2 Log the value of module01.module - this refers to the module exported from module01.
 */
console.log( '\n*** module02 execution starts ***' );

// *** start writing code after this line ***





// *** end your code before this line ***

console.log( '*** module02 execution ends ***\n' );