/**
 * The querystring module has methods for working with a querystring
 * 
 * Instructions:
 * 1. Require module 'querystring'
 * 2. The module's parse() method parses a query string - use it and check the parsed query string.
 * 3. Use the module's stringify() method to construct the query string from its parts.
 */

const amazonUrlQueryString = 'url=search-alias%3Daps&field-keywords=mobile+phone';