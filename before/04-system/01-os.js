/**
 * The 'os' module is used to retrieve info about the underlying OS.
 * 
 * Instructions:
 * 1. Require 'os' module
 * 2. Check cpus(), networkInterfaces(), totalmem(), freemem(, type(), userInfo() and constants property)
 */